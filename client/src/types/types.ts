export type RecordItem = {
    id: number;
    description: string;
    amount: number;
    category: string;
    created_at: string;
};

export type DataItem = {
    name: string;
    value: number;
};
